/**
 * @deprecated Downloads use assessmentPacketPdfFill (official AcroForm templates).
 * Kept only if a caller still imports HTML rendering helpers.
 */
export {
  fillAssessmentPacketPdf,
  fillAssessmentPacketAllPdfs,
  openPdfBytes,
} from './assessmentPacketPdfFill';
