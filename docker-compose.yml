services:
  admin:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
    image: caretracker-admin:latest
    container_name: caretracker-admin
    ports:
      - "${ADMIN_PORT:-5137}:5137"
    restart: unless-stopped
