services:
  admin:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL}
    image: caretracker-admin:latest
    container_name: caretracker-admin
    ports:
      - "${ADMIN_PORT:-5173}:5173"
    restart: unless-stopped
